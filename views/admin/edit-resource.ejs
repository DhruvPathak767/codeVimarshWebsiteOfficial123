<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= pageTitle %>
  </title>
  <link rel="stylesheet" href="/output.css">
  <link rel="stylesheet" href="/css/home.css">
  <link rel="stylesheet" href="/css/admin.css">
  <link rel="stylesheet" href="/css/buttons.css">
  <link rel="stylesheet" href="/css/animations.css">
  <link
    href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Outfit:wght@300;400;600;800&display=swap"
    rel="stylesheet">
</head>

<body class="antialiased selection:bg-primary selection:text-black">
  <canvas id="bg-canvas"></canvas>

  <div class="relative z-10 flex flex-col min-h-screen">
    <%- include('../partials/nav.ejs') %>

      <main class="flex-grow pt-24 pb-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-3xl mx-auto">
          <div class="mb-12">
            <a href="/admin/resources"
              class="text-text-muted hover:text-white transition-colors text-sm font-code flex items-center gap-2 mb-4">
              &larr; Back to Resources
            </a>
            <h1 class="text-3xl md:text-4xl font-display font-bold">
              Edit <span class="text-gradient">Resource</span>
            </h1>
          </div>

          <!-- Edit Resource Form -->
          <div class="glass-effect p-8 rounded-2xl border border-white/10 mb-12 animate-fade-up">
            <form action="/admin/resources/edit" method="POST" enctype="multipart/form-data"
              class="grid grid-cols-1 md:grid-cols-2 gap-6">

              <input type="hidden" name="resourceId" value="<%= resource._id %>">

              <!-- Title -->
              <div class="form-group">
                <input type="text" id="title" name="title" class="form-input" placeholder=" "
                  value="<%= resource.title %>" required>
                <label for="title" class="form-label">Title</label>
              </div>

              <!-- Category -->
              <div class="form-group">
                <select id="category" name="category" class="form-input" required>
                  <option value="dsa" <%=resource.category==='dsa' ? 'selected' : '' %>>DSA Sheet</option>
                  <option value="playlist" <%=resource.category==='playlist' ? 'selected' : '' %>>Youtube Playlist
                  </option>
                  <option value="other" <%=resource.category==='other' ? 'selected' : '' %>>Other</option>
                </select>
                <label for="category" class="form-label">Category</label>
              </div>

              <!-- Description -->
              <div class="form-group md:col-span-2">
                <textarea id="description" name="description" class="form-input" rows="3" placeholder=" "
                  required><%= resource.description %></textarea>
                <label for="description" class="form-label">Description</label>
              </div>

              <!-- Link -->
              <div class="form-group">
                <input type="url" id="link" name="link" class="form-input" placeholder=" " value="<%= resource.link %>"
                  required>
                <label for="link" class="form-label">Resource Link</label>
              </div>

              <!-- Current Image Preview & Upload -->


              <div class="form-group">
                <input type="file" id="image" name="image" class="form-input" style="padding-top: 1rem; height: auto;">
                <label for="image" class="form-label"
                  style="top: -0.5rem; left: 0.75rem; font-size: 0.75rem; background: #0f172a; padding: 0 0.5rem; color: #08d9d6;">Start
                  typing to Replace Image (Optional)</label>
              </div>
          </div>


          <div class="md:col-span-2 flex gap-4 mt-4">
            <button type="submit"
              class="btn-cyber px-8 py-3 w-full md:w-auto rounded-sm text-sm font-bold uppercase tracking-wider flex-grow md:flex-grow-0 text-center">
              Update Resource
            </button>
            <a href="/admin/resources"
              class="px-8 py-3 w-full md:w-auto rounded-sm text-sm font-bold uppercase tracking-wider border border-white/20 hover:bg-white/5 text-white transition-all text-center">
              Cancel
            </a>
          </div>
          </form>
        </div>

  </div>
  </main>

  <%- include('../partials/footer.ejs') %>
    </div>
    <script src="/js/home.js"></script>
</body>

</html>